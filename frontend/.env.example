# NOTE: VITE_API_URL is set in dev.nix automatically
# You typically don't need a frontend/.env file in Firebase Studio

# Only create this file if running outside Firebase Studio:
# VITE_API_URL=http://localhost:8000
